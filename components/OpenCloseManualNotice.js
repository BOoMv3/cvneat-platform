'use client';

/**
 * Ancien bandeau « ouverture / fermeture manuelle » (bug horaires).
 * L’ouverture affichée suit désormais les plages horaires ; ce composant ne s’affiche plus.
 */
export default function OpenCloseManualNotice() {
  return null;
}
